These migrations are historical and are retained only to interpret existing database exports.
Do not apply them to a standalone database. The supported migrations are now in
`db/migrations`, applied with `npm run db:migrate`. See `docs/POSTGRESQL.md`.
